#include <stdio.h>

int main(void) {
    //12 numeros, na sequencia 4, 8, 12, 16 ...
    int n = 4;
    for (int i = 1; i <= 12; i++) {
        printf("%d\n", n);
        n+=4;
    }
    for (int i=4; i<=(12*4); i+=4) {
        printf("%d\n", i);
    }
    return 0;
}