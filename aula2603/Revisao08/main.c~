#include <stdio.h>

int main(void) {
    int num;
    scanf("%d", &num);
    if (num % 2 == 0) {
        printf("Numero eh par\n");
    }else {
        printf("Numero eh impar\n");
    }
    return 0;
}