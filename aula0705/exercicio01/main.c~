//contagem regressiva de N até 0
#include <stdio.h>

int main(void) {
    int n;
    printf("Digite o valor inicial: [Maior que zero]\n");
    scanf("%d", &n);
    while (n >= 0) {
        printf("%d\n", n--);
    }
    return 0;
}